#pragma once

/**
 * @class Client
 * @brief A small helper class representing an external client or UI layer.
 *
 * This can be used by main() or tests to interact with the Nursery facade.
 */
class Client {
   public:
    Client();
    ~Client() = default;
};
